$("#modal").modal("show");
$("#modal .modal-header h3").html("<%= t('layout.member') + ':' +  @member.name %>");
$("#modal .modal-body").html("<%= j render template: 'members/show', formats: [:html], handler: [:erb] %>");
